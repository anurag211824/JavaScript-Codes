true ? "anurag" : "abhinav";

const user = 5 > 2 ? "anurag" : `ujjawal`;
console.log(user);

const gender = "M";

const userMessage = `${gender === "F" ? "She" : "He"} is a college student`;
console.log(userMessage);
